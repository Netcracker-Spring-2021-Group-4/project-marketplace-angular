export enum Route {
  LOGIN = "login",
  REGISTER ="register",
  CONFIRM_TOKEN = "confirm-token",
  PASSWORD = "password",
  CATALOG = "catalog",
  PRODUCT = "product",
  COMPARE = "compare",
  CART = "cart",
  CHECK_OUT = "check-out",
}
