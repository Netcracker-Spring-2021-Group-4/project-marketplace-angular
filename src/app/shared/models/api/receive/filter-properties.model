import {Category} from "./category";

export class FilterProperties {
  categories: Category[];
  maxPrice: number;

  constructor(init: Partial<FilterProperties>) {
    Object.assign(this, init);
  }
}
